name: Selenium Tests

on:
  push:
    branches: [ "ci-cd-tests" ]
  pull_request:
    branches: [ "ci-cd-tests" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v4

    - name: Instalar Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
        cache: 'pip' # Esto hace que las siguientes veces instale más rápido

    - name: Instalar dependencias
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Instalar Chrome
      uses: browser-actions/setup-chrome@v1
      with:
        chrome-version: stable

    - name: Ejecutar tests
      # Agregamos env: CI: true para que tu conftest.py lo detecte correctamente
      env:
        CI: "true"
      run: pytest tests/test_reporte.py -s

    - name: Subir reportes
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: reportes-ejecucion
        path: |
          reports/
          screenshots/
          downloads/
    - name: Instalar wkhtmltopdf (Linux)
      run: |
        sudo apt-get update
        sudo apt-get install -y wkhtmltopdf

    - name: Ejecutar Tests
      run: pytest tests/test_reporte.py -s

    - name: Generar PDF Consolidado
      if: always()
      run: python utils/pdf_merger.py

    - name: Comprimir en ZIP
      if: always()
      run: python utils/zipper.py

    - name: Subir Reporte Final (Artefactos)
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: Paquete-Auditoria-Final
        path: "*.zip" # Sube cualquier archivo ZIP que encuentre en la raíz